name: Check PEP8

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Check PEP8
      run: docker run --rm -v ./bot:/code eeacms/pep8
